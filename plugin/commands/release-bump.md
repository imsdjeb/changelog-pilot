---
description: "Bump the version in all manifest files (package.json, pubspec.yaml, Cargo.toml, etc.)."
argument-hint: "[major|minor|patch|version-number] e.g. minor or 2.0.0"
---

Run the **BUMP** workflow from the changelog-pilot skill. Calculate the recommended version bump (or use the provided override), update the version in all relevant manifest files, and commit the change.
