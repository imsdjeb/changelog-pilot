---
description: "Create an annotated git tag for the current version and optionally push it."
argument-hint: "[version] e.g. 1.3.0"
---

Run the **TAG** workflow from the changelog-pilot skill. Verify the working directory is clean, create an annotated git tag, and offer to push it to the remote.
